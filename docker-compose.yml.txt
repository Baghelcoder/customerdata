version: '3'
services:
  backend:
    container_name: backend
    build: ./backend
    ports:
      - "1001:1001"
    depends_on:
      - database

  frontend:
    container_name: frontend
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  database:
    container_name: database
    image: mongo
    ports:
      - "27017:27017"